{
  "name": "@chronos/adapters",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "files": [
    "dist"
  ],
  "exports": "./dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test test/chronos-jsonl.test.mjs test/provider-jsonl.test.mjs test/redaction.test.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.test.json"
  },
  "dependencies": {
    "@chronos/protocol": "0.2.0"
  },
  "devDependencies": {
    "@chronos/core": "0.2.0"
  }
}
